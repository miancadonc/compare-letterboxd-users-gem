RSpec.describe CompareLetterboxdUsersGem do
  it "has a version number" do
    expect(CompareLetterboxdUsersGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
